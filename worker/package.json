{
  "name": "stallpos-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types",
    "typecheck": "npm run cf-typegen && tsc --noEmit -p tsconfig.json && tsc --noEmit -p test/tsconfig.json",
    "test": "vitest run",
    "restore-drill": "bash scripts/restore-drill.sh",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@cfworker/json-schema": "4.1.1",
    "@sentry/cloudflare": "10.70.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.0.0",
    "@cloudflare/workers-types": "5.20260823.1",
    "@types/node": "^26.2.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  }
}
